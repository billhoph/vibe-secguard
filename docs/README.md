# docs assets

Place the demo screenshot here as `demo.png` (referenced by the top-level README).
Suggested: a combined screenshot showing the `/secscan` static summary alongside
an OWASP ZAP `/dast` report.
